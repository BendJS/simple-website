import React from "react";
import { StyledFooter, FlexDiv } from "../UI/Common";

export default function Footer(props) {
  return (
    <StyledFooter>
      <FlexDiv justify="flex-start" align="center">
        Our little JavaScript community in Bend, Oregon.
      </FlexDiv>
    </StyledFooter>
  );
}
